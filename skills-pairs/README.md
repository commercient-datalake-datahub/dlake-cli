# Generated pair skills for the `dlake` CLI

One skill per **ERP × CRM pair** that ships a Standard CRMPro template set, generated from the shipped template
catalogue. Each page states only what its pair's templates set up — the process rows, the views and how each
detects change, the repository key, the order of work, the field mapping and the business outcome of each
template group — and defers to its parent, [`skills/dlake-crmpro/SKILL.md`](../skills/dlake-crmpro/SKILL.md),
for the tools, the tables and the engine behaviour. Where a hand-written destination skill exists
(`dlake-crmpro-hubspot`, `dlake-crmpro-syspro-salesforce`, `dlake-crmpro-syspro-shopify` under `skills/`),
the pair page names it too. Pages describe Standard templates only and quote no template SQL.

These pages are **not bundled in the CLI** and `dlake skills install` does not install them. Fetch the one you
need: copy its folder into your harness's skills directory, or read it here. Every page carries
`generated: true` and a marker comment; a hand-edit promotes a page to hand-written status and it is then
reviewed line by line. The pages grow as the catalogue does; a thin page reflects a thin template set.

How they are produced: [`skills/making-erp-crm-skill-pages/SKILL.md`](../skills/making-erp-crm-skill-pages/SKILL.md).

One row per generated page, in the style of the **Available skills** table in
`publishing/dlake-cli/skills/README.md`, grouped by destination. Each page is generated from the
shipped template catalogue and extends `dlake-crmpro/`; the hand-written destination pages
(`dlake-crmpro-hubspot/`, `dlake-crmpro-syspro-salesforce/`, `dlake-crmpro-syspro-shopify/`) keep their
own rows and are not repeated here.

## DynamicCRM

| Skill | What it covers |
|-------|----------------|
| [`dlake-crmpro-epicor10-dynamiccrm/`](dlake-crmpro-epicor10-dynamiccrm/SKILL.md) | **DynamicCRM** specifics for a CRMPro forward sync from an EPICOR 10 source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |

## HubSpot

| Skill | What it covers |
|-------|----------------|
| [`dlake-crmpro-acumatica-hubspot/`](dlake-crmpro-acumatica-hubspot/SKILL.md) | **HubSpot** specifics for a CRMPro forward sync from an ACUMATICA source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-acumaticacloud-hubspot/`](dlake-crmpro-acumaticacloud-hubspot/SKILL.md) | **HubSpot** specifics for a CRMPro forward sync from an ACUMATICA CLOUD source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-apteanintuitive-hubspot/`](dlake-crmpro-apteanintuitive-hubspot/SKILL.md) | **HubSpot** specifics for a CRMPro forward sync from an APTEAN INTUITIVE source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-apteanross-hubspot/`](dlake-crmpro-apteanross-hubspot/SKILL.md) | **HubSpot** specifics for a CRMPro forward sync from an APTEAN ROSS source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-apteanworkwise-hubspot/`](dlake-crmpro-apteanworkwise-hubspot/SKILL.md) | **HubSpot** specifics for a CRMPro forward sync from an APTEAN WORKWISE source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-datacorchempax-hubspot/`](dlake-crmpro-datacorchempax-hubspot/SKILL.md) | **HubSpot** specifics for a CRMPro forward sync from a DATACOR-CHEMPAX source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-deltek-hubspot/`](dlake-crmpro-deltek-hubspot/SKILL.md) | **HubSpot** specifics for a CRMPro forward sync from a DELTEK VISION source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-deltekajera-hubspot/`](dlake-crmpro-deltekajera-hubspot/SKILL.md) | **HubSpot** specifics for a CRMPro forward sync from a DELTEK AJERA source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-dynamicsbusinesscentral-hubspot/`](dlake-crmpro-dynamicsbusinesscentral-hubspot/SKILL.md) | **HubSpot** specifics for a CRMPro forward sync from a DYNAMICS BUSINESS CENTRAL source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-epicor10-hubspot/`](dlake-crmpro-epicor10-hubspot/SKILL.md) | **HubSpot** specifics for a CRMPro forward sync from an EPICOR 10 source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-epicor11kinetic-hubspot/`](dlake-crmpro-epicor11kinetic-hubspot/SKILL.md) | **HubSpot** specifics for a CRMPro forward sync from an EPICOR 11 KINETIC source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-epicorbistrack-hubspot/`](dlake-crmpro-epicorbistrack-hubspot/SKILL.md) | **HubSpot** specifics for a CRMPro forward sync from an EPICOR BISTRACK source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-epicorcloud-hubspot/`](dlake-crmpro-epicorcloud-hubspot/SKILL.md) | **HubSpot** specifics for a CRMPro forward sync from an EPICOR CLOUD source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-epicorkinect-hubspot/`](dlake-crmpro-epicorkinect-hubspot/SKILL.md) | **HubSpot** specifics for a CRMPro forward sync from an EPICORKINECT source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-epicorkinetic-hubspot/`](dlake-crmpro-epicorkinetic-hubspot/SKILL.md) | **HubSpot** specifics for a CRMPro forward sync from an EPICORKINETIC source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-epicorp21-hubspot/`](dlake-crmpro-epicorp21-hubspot/SKILL.md) | **HubSpot** specifics for a CRMPro forward sync from an EPICOR P21(PROPHET 21) source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-exactmax-hubspot/`](dlake-crmpro-exactmax-hubspot/SKILL.md) | **HubSpot** specifics for a CRMPro forward sync from an EXACT MAX source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-exactonline-hubspot/`](dlake-crmpro-exactonline-hubspot/SKILL.md) | **HubSpot** specifics for a CRMPro forward sync from an EXACT ONLINE source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-globalshop-hubspot/`](dlake-crmpro-globalshop-hubspot/SKILL.md) | **HubSpot** specifics for a CRMPro forward sync from a GLOBALSHOP source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-hubspot-hubspot/`](dlake-crmpro-hubspot-hubspot/SKILL.md) | **HubSpot** specifics for a CRMPro forward sync from a HubSpot source, from the shipped templates: the configuration values each template inserts and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-ifs-hubspot/`](dlake-crmpro-ifs-hubspot/SKILL.md) | **HubSpot** specifics for a CRMPro forward sync from an IFS source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-infor10distributionbusiness-hubspot/`](dlake-crmpro-infor10distributionbusiness-hubspot/SKILL.md) | **HubSpot** specifics for a CRMPro forward sync from an INFOR10 DISTRIBUTION BUSINESS source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-inforcloudsuite-hubspot/`](dlake-crmpro-inforcloudsuite-hubspot/SKILL.md) | **HubSpot** specifics for a CRMPro forward sync from an INFOR CLOUDSUITE source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-inforcsd-hubspot/`](dlake-crmpro-inforcsd-hubspot/SKILL.md) | **HubSpot** specifics for a CRMPro forward sync from an INFOR CSD source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-inforfacts-hubspot/`](dlake-crmpro-inforfacts-hubspot/SKILL.md) | **HubSpot** specifics for a CRMPro forward sync from an INFOR FACTS source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-inforsxe-hubspot/`](dlake-crmpro-inforsxe-hubspot/SKILL.md) | **HubSpot** specifics for a CRMPro forward sync from an INFOR SXE source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-inforsytelinev9-hubspot/`](dlake-crmpro-inforsytelinev9-hubspot/SKILL.md) | **HubSpot** specifics for a CRMPro forward sync from an INFOR SYTELINE V9 source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-inforvisual-hubspot/`](dlake-crmpro-inforvisual-hubspot/SKILL.md) | **HubSpot** specifics for a CRMPro forward sync from an INFOR VISUAL source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-inforvisual9-hubspot/`](dlake-crmpro-inforvisual9-hubspot/SKILL.md) | **HubSpot** specifics for a CRMPro forward sync from an INFORVISUAL9 source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-jobboss-hubspot/`](dlake-crmpro-jobboss-hubspot/SKILL.md) | **HubSpot** specifics for a CRMPro forward sync from a JOBBOSS source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-macola10-hubspot/`](dlake-crmpro-macola10-hubspot/SKILL.md) | **HubSpot** specifics for a CRMPro forward sync from a MACOLA10 source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-macolaes-hubspot/`](dlake-crmpro-macolaes-hubspot/SKILL.md) | **HubSpot** specifics for a CRMPro forward sync from a MACOLAES source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-macolaprogrssion-hubspot/`](dlake-crmpro-macolaprogrssion-hubspot/SKILL.md) | **HubSpot** specifics for a CRMPro forward sync from a MACOLAPROGRSSION source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-microsoftbusinesscentral-hubspot/`](dlake-crmpro-microsoftbusinesscentral-hubspot/SKILL.md) | **HubSpot** specifics for a CRMPro forward sync from a MICROSOFT BUSINESS CENTRAL source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-msdynamicax-hubspot/`](dlake-crmpro-msdynamicax-hubspot/SKILL.md) | **HubSpot** specifics for a CRMPro forward sync from a MS DYNAMIC AX source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-msdynamicgp2016-hubspot/`](dlake-crmpro-msdynamicgp2016-hubspot/SKILL.md) | **HubSpot** specifics for a CRMPro forward sync from a MS DYNAMIC GP 2016 source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-msdynamicgp2017-hubspot/`](dlake-crmpro-msdynamicgp2017-hubspot/SKILL.md) | **HubSpot** specifics for a CRMPro forward sync from a MS DYNAMIC GP 2017 source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-msdynamicnav-hubspot/`](dlake-crmpro-msdynamicnav-hubspot/SKILL.md) | **HubSpot** specifics for a CRMPro forward sync from a MS DYNAMIC NAV source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-myobaccountright-hubspot/`](dlake-crmpro-myobaccountright-hubspot/SKILL.md) | **HubSpot** specifics for a CRMPro forward sync from a MYOB ACCOUNTRIGHT source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-netsuite-hubspot/`](dlake-crmpro-netsuite-hubspot/SKILL.md) | **HubSpot** specifics for a CRMPro forward sync from a NETSUITE source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-plex-hubspot/`](dlake-crmpro-plex-hubspot/SKILL.md) | **HubSpot** specifics for a CRMPro forward sync from a PLEX source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-processpro-hubspot/`](dlake-crmpro-processpro-hubspot/SKILL.md) | **HubSpot** specifics for a CRMPro forward sync from a PROCESS PRO source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-quickbook-hubspot/`](dlake-crmpro-quickbook-hubspot/SKILL.md) | **HubSpot** specifics for a CRMPro forward sync from a QUICKBOOKS source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-quickbooks-hubspot/`](dlake-crmpro-quickbooks-hubspot/SKILL.md) | **HubSpot** specifics for a CRMPro forward sync from a QUICKBOOKS source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-sage100-hubspot/`](dlake-crmpro-sage100-hubspot/SKILL.md) | **HubSpot** specifics for a CRMPro forward sync from a SAGE 100 ERP US source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-sage1002013v5-hubspot/`](dlake-crmpro-sage1002013v5-hubspot/SKILL.md) | **HubSpot** specifics for a CRMPro forward sync from a SAGE1002013V5 source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-sage1002014-hubspot/`](dlake-crmpro-sage1002014-hubspot/SKILL.md) | **HubSpot** specifics for a CRMPro forward sync from a SAGE1002014 source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-sage1002015-hubspot/`](dlake-crmpro-sage1002015-hubspot/SKILL.md) | **HubSpot** specifics for a CRMPro forward sync from a SAGE1002015 source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-sage1002016-hubspot/`](dlake-crmpro-sage1002016-hubspot/SKILL.md) | **HubSpot** specifics for a CRMPro forward sync from a SAGE1002016 source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-sage1002017-hubspot/`](dlake-crmpro-sage1002017-hubspot/SKILL.md) | **HubSpot** specifics for a CRMPro forward sync from a SAGE1002017 source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-sage100contractor2019-hubspot/`](dlake-crmpro-sage100contractor2019-hubspot/SKILL.md) | **HubSpot** specifics for a CRMPro forward sync from a SAGE100CONTRACTOR 2019 source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-sage100germany-hubspot/`](dlake-crmpro-sage100germany-hubspot/SKILL.md) | **HubSpot** specifics for a CRMPro forward sync from a SAGE 100 GERMANY source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-sage100us-hubspot/`](dlake-crmpro-sage100us-hubspot/SKILL.md) | **HubSpot** specifics for a CRMPro forward sync from a SAGE100US source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-sage200uk-hubspot/`](dlake-crmpro-sage200uk-hubspot/SKILL.md) | **HubSpot** specifics for a CRMPro forward sync from a SAGE 200 UK source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-sage200us-hubspot/`](dlake-crmpro-sage200us-hubspot/SKILL.md) | **HubSpot** specifics for a CRMPro forward sync from a SAGE 200 US source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-sage300-hubspot/`](dlake-crmpro-sage300-hubspot/SKILL.md) | **HubSpot** specifics for a CRMPro forward sync from a SAGE 300 source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-sage300cre-hubspot/`](dlake-crmpro-sage300cre-hubspot/SKILL.md) | **HubSpot** specifics for a CRMPro forward sync from a SAGE 300 CRE source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-sage500-hubspot/`](dlake-crmpro-sage500-hubspot/SKILL.md) | **HubSpot** specifics for a CRMPro forward sync from a SAGE 500 source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-sage50cloud-hubspot/`](dlake-crmpro-sage50cloud-hubspot/SKILL.md) | **HubSpot** specifics for a CRMPro forward sync from a SAGE 50 CLOUD source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-sage50uk-hubspot/`](dlake-crmpro-sage50uk-hubspot/SKILL.md) | **HubSpot** specifics for a CRMPro forward sync from a SAGE 50 UK source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-sage50us-hubspot/`](dlake-crmpro-sage50us-hubspot/SKILL.md) | **HubSpot** specifics for a CRMPro forward sync from a SAGE 50 US source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-sagex3-hubspot/`](dlake-crmpro-sagex3-hubspot/SKILL.md) | **HubSpot** specifics for a CRMPro forward sync from a SAGE X3 source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-sapb1-hubspot/`](dlake-crmpro-sapb1-hubspot/SKILL.md) | **HubSpot** specifics for a CRMPro forward sync from a SAP B1 source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-sapbusinessbydesign-hubspot/`](dlake-crmpro-sapbusinessbydesign-hubspot/SKILL.md) | **HubSpot** specifics for a CRMPro forward sync from a SAP BUSINESS BYDESIGN source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-southware-hubspot/`](dlake-crmpro-southware-hubspot/SKILL.md) | **HubSpot** specifics for a CRMPro forward sync from a SOUTHWARE source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-steelviking-hubspot/`](dlake-crmpro-steelviking-hubspot/SKILL.md) | **HubSpot** specifics for a CRMPro forward sync from a STEELVIKING source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-syspro7-hubspot/`](dlake-crmpro-syspro7-hubspot/SKILL.md) | **HubSpot** specifics for a CRMPro forward sync from a SYSPRO 7 and Above source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-vais2k-hubspot/`](dlake-crmpro-vais2k-hubspot/SKILL.md) | **HubSpot** specifics for a CRMPro forward sync from a VAI S2K source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |

## MDC

| Skill | What it covers |
|-------|----------------|
| [`dlake-crmpro-apteanmade2manage-mdc/`](dlake-crmpro-apteanmade2manage-mdc/SKILL.md) | **MDC** specifics for a CRMPro forward sync from an APTEAN MADE2MANAGE source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-baan-mdc/`](dlake-crmpro-baan-mdc/SKILL.md) | **MDC** specifics for a CRMPro forward sync from a BAAN source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-epicoreagle-mdc/`](dlake-crmpro-epicoreagle-mdc/SKILL.md) | **MDC** specifics for a CRMPro forward sync from an EPICOR EAGLE source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-ifserp-mdc/`](dlake-crmpro-ifserp-mdc/SKILL.md) | **MDC** specifics for a CRMPro forward sync from an IFS source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-inforsytelinev9-mdc/`](dlake-crmpro-inforsytelinev9-mdc/SKILL.md) | **MDC** specifics for a CRMPro forward sync from an INFOR SYTELINE V9 source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-sage100-mdc/`](dlake-crmpro-sage100-mdc/SKILL.md) | **MDC** specifics for a CRMPro forward sync from a SAGE 100 ERP US source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-sage1002017-mdc/`](dlake-crmpro-sage1002017-mdc/SKILL.md) | **MDC** specifics for a CRMPro forward sync from a SAGE1002017 source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-sage100us-mdc/`](dlake-crmpro-sage100us-mdc/SKILL.md) | **MDC** specifics for a CRMPro forward sync from a SAGE100US source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-sage50uk-mdc/`](dlake-crmpro-sage50uk-mdc/SKILL.md) | **MDC** specifics for a CRMPro forward sync from a SAGE 50 UK source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-sapb1-mdc/`](dlake-crmpro-sapb1-mdc/SKILL.md) | **MDC** specifics for a CRMPro forward sync from a SAP B1 source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-syspro7-mdc/`](dlake-crmpro-syspro7-mdc/SKILL.md) | **MDC** specifics for a CRMPro forward sync from a SYSPRO 7 and Above source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |

## MEGENTO

| Skill | What it covers |
|-------|----------------|
| [`dlake-crmpro-epicor10-megento/`](dlake-crmpro-epicor10-megento/SKILL.md) | **MEGENTO** specifics for a CRMPro forward sync from an EPICOR 10 source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-msdynamicgp2017-megento/`](dlake-crmpro-msdynamicgp2017-megento/SKILL.md) | **MEGENTO** specifics for a CRMPro forward sync from a MS DYNAMIC GP 2017 source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-sage50uk-megento/`](dlake-crmpro-sage50uk-megento/SKILL.md) | **MEGENTO** specifics for a CRMPro forward sync from a SAGE 50 UK source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-syspro7-megento/`](dlake-crmpro-syspro7-megento/SKILL.md) | **MEGENTO** specifics for a CRMPro forward sync from a SYSPRO 7 and Above source, from the shipped templates: the configuration values each template inserts. Extends `dlake-crmpro/`. |

## Salesforce

| Skill | What it covers |
|-------|----------------|
| [`dlake-crmpro-acumatica-salesforce/`](dlake-crmpro-acumatica-salesforce/SKILL.md) | **Salesforce** specifics for a CRMPro forward sync from an ACUMATICA source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-acumaticacloud-salesforce/`](dlake-crmpro-acumaticacloud-salesforce/SKILL.md) | **Salesforce** specifics for a CRMPro forward sync from an ACUMATICA CLOUD source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-apteanencompix-salesforce/`](dlake-crmpro-apteanencompix-salesforce/SKILL.md) | **Salesforce** specifics for a CRMPro forward sync from an APTEAN ENCOMPIX source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-apteanintuitive-salesforce/`](dlake-crmpro-apteanintuitive-salesforce/SKILL.md) | **Salesforce** specifics for a CRMPro forward sync from an APTEAN INTUITIVE source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-apteanmade2manage-salesforce/`](dlake-crmpro-apteanmade2manage-salesforce/SKILL.md) | **Salesforce** specifics for a CRMPro forward sync from an APTEAN MADE2MANAGE source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-apteanross-salesforce/`](dlake-crmpro-apteanross-salesforce/SKILL.md) | **Salesforce** specifics for a CRMPro forward sync from an APTEAN ROSS source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-deltek-salesforce/`](dlake-crmpro-deltek-salesforce/SKILL.md) | **Salesforce** specifics for a CRMPro forward sync from a DELTEK VISION source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-deltekvantagepoint-salesforce/`](dlake-crmpro-deltekvantagepoint-salesforce/SKILL.md) | **Salesforce** specifics for a CRMPro forward sync from a DELTEK VANTAGEPOINT source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-dynamicsbusinesscentral-salesforce/`](dlake-crmpro-dynamicsbusinesscentral-salesforce/SKILL.md) | **Salesforce** specifics for a CRMPro forward sync from a DYNAMICS BUSINESS CENTRAL source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-ecieautomate-salesforce/`](dlake-crmpro-ecieautomate-salesforce/SKILL.md) | **Salesforce** specifics for a CRMPro forward sync from an ECI E-AUTOMATE source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-ecim1-salesforce/`](dlake-crmpro-ecim1-salesforce/SKILL.md) | **Salesforce** specifics for a CRMPro forward sync from an ECI M1 source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-epicor10-salesforce/`](dlake-crmpro-epicor10-salesforce/SKILL.md) | **Salesforce** specifics for a CRMPro forward sync from an EPICOR 10 source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-epicor10cloud-salesforce/`](dlake-crmpro-epicor10cloud-salesforce/SKILL.md) | **Salesforce** specifics for a CRMPro forward sync from an EPICOR10 CLOUD source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-epicor11kinetic-salesforce/`](dlake-crmpro-epicor11kinetic-salesforce/SKILL.md) | **Salesforce** specifics for a CRMPro forward sync from an EPICOR 11 KINETIC source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-epicor9-salesforce/`](dlake-crmpro-epicor9-salesforce/SKILL.md) | **Salesforce** specifics for a CRMPro forward sync from an EPICOR 9, EPICOR 9.5 source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-epicorp21-salesforce/`](dlake-crmpro-epicorp21-salesforce/SKILL.md) | **Salesforce** specifics for a CRMPro forward sync from an EPICOR P21(PROPHET 21) source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-esolver-salesforce/`](dlake-crmpro-esolver-salesforce/SKILL.md) | **Salesforce** specifics for a CRMPro forward sync from an E-SOLVER source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-exactglobenext-salesforce/`](dlake-crmpro-exactglobenext-salesforce/SKILL.md) | **Salesforce** specifics for a CRMPro forward sync from an EXACT GLOBE NEXT source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-exactmax-salesforce/`](dlake-crmpro-exactmax-salesforce/SKILL.md) | **Salesforce** specifics for a CRMPro forward sync from an EXACT MAX source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-famous-salesforce/`](dlake-crmpro-famous-salesforce/SKILL.md) | **Salesforce** specifics for a CRMPro forward sync from a FAMOUS source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-globalshop-salesforce/`](dlake-crmpro-globalshop-salesforce/SKILL.md) | **Salesforce** specifics for a CRMPro forward sync from a GLOBALSHOP source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-globalshop2020-salesforce/`](dlake-crmpro-globalshop2020-salesforce/SKILL.md) | **Salesforce** specifics for a CRMPro forward sync from a GLOBALSHOP2020 source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-ifserp-salesforce/`](dlake-crmpro-ifserp-salesforce/SKILL.md) | **Salesforce** specifics for a CRMPro forward sync from an IFS source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-infor10distributionbusiness-salesforce/`](dlake-crmpro-infor10distributionbusiness-salesforce/SKILL.md) | **Salesforce** specifics for a CRMPro forward sync from an INFOR10 DISTRIBUTION BUSINESS source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-infora-salesforce/`](dlake-crmpro-infora-salesforce/SKILL.md) | **Salesforce** specifics for a CRMPro forward sync from an INFOR A+ source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-inforcloudsuite-salesforce/`](dlake-crmpro-inforcloudsuite-salesforce/SKILL.md) | **Salesforce** specifics for a CRMPro forward sync from an INFOR CLOUDSUITE source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-inforfourthshift-salesforce/`](dlake-crmpro-inforfourthshift-salesforce/SKILL.md) | **Salesforce** specifics for a CRMPro forward sync from an INFOR FOURTH SHIFT source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-inforln10-salesforce/`](dlake-crmpro-inforln10-salesforce/SKILL.md) | **Salesforce** specifics for a CRMPro forward sync from an INFOR LN 10 source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-inforln6-salesforce/`](dlake-crmpro-inforln6-salesforce/SKILL.md) | **Salesforce** specifics for a CRMPro forward sync from an INFOR LN 6 source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-inform3-salesforce/`](dlake-crmpro-inform3-salesforce/SKILL.md) | **Salesforce** specifics for a CRMPro forward sync from an INFOR M3 source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-inform3qm-salesforce/`](dlake-crmpro-inform3qm-salesforce/SKILL.md) | **Salesforce** specifics for a CRMPro forward sync from an INFOR M3 QM source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-inforsxe-salesforce/`](dlake-crmpro-inforsxe-salesforce/SKILL.md) | **Salesforce** specifics for a CRMPro forward sync from an INFOR SXE source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-inforsytelinev705-salesforce/`](dlake-crmpro-inforsytelinev705-salesforce/SKILL.md) | **Salesforce** specifics for a CRMPro forward sync from an INFOR SYTELINE V7 & V8 source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-inforsytelinev9-salesforce/`](dlake-crmpro-inforsytelinev9-salesforce/SKILL.md) | **Salesforce** specifics for a CRMPro forward sync from an INFOR SYTELINE V9 source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-inforvisual-salesforce/`](dlake-crmpro-inforvisual-salesforce/SKILL.md) | **Salesforce** specifics for a CRMPro forward sync from an INFOR VISUAL source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-inforvisual712-salesforce/`](dlake-crmpro-inforvisual712-salesforce/SKILL.md) | **Salesforce** specifics for a CRMPro forward sync from an INFOR VISUAL 7.1.2 source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-inforvisual9-salesforce/`](dlake-crmpro-inforvisual9-salesforce/SKILL.md) | **Salesforce** specifics for a CRMPro forward sync from an INFORVISUAL9 source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-inforxa-salesforce/`](dlake-crmpro-inforxa-salesforce/SKILL.md) | **Salesforce** specifics for a CRMPro forward sync from an INFOR XA source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-iqms-salesforce/`](dlake-crmpro-iqms-salesforce/SKILL.md) | **Salesforce** specifics for a CRMPro forward sync from an IQMS source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-jdedwards-salesforce/`](dlake-crmpro-jdedwards-salesforce/SKILL.md) | **Salesforce** specifics for a CRMPro forward sync from a JD EDWARDS source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-jobboss-salesforce/`](dlake-crmpro-jobboss-salesforce/SKILL.md) | **Salesforce** specifics for a CRMPro forward sync from a JOBBOSS source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-macola10-salesforce/`](dlake-crmpro-macola10-salesforce/SKILL.md) | **Salesforce** specifics for a CRMPro forward sync from a MACOLA10 source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-microsoftbusinesscentral-salesforce/`](dlake-crmpro-microsoftbusinesscentral-salesforce/SKILL.md) | **Salesforce** specifics for a CRMPro forward sync from a MICROSOFT BUSINESS CENTRAL source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-msdynamicax-salesforce/`](dlake-crmpro-msdynamicax-salesforce/SKILL.md) | **Salesforce** specifics for a CRMPro forward sync from a MS DYNAMIC AX source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-msdynamicgp2016-salesforce/`](dlake-crmpro-msdynamicgp2016-salesforce/SKILL.md) | **Salesforce** specifics for a CRMPro forward sync from a MS DYNAMIC GP 2016 source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-msdynamicgp2017-salesforce/`](dlake-crmpro-msdynamicgp2017-salesforce/SKILL.md) | **Salesforce** specifics for a CRMPro forward sync from a MS DYNAMIC GP 2017 source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-msdynamicnav-salesforce/`](dlake-crmpro-msdynamicnav-salesforce/SKILL.md) | **Salesforce** specifics for a CRMPro forward sync from a MS DYNAMIC NAV source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-msdynamicsl2015-salesforce/`](dlake-crmpro-msdynamicsl2015-salesforce/SKILL.md) | **Salesforce** specifics for a CRMPro forward sync from a MS DYNAMIC SL 2015 source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-myobaccountright-salesforce/`](dlake-crmpro-myobaccountright-salesforce/SKILL.md) | **Salesforce** specifics for a CRMPro forward sync from a MYOB ACCOUNTRIGHT source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-myobadvanced-salesforce/`](dlake-crmpro-myobadvanced-salesforce/SKILL.md) | **Salesforce** specifics for a CRMPro forward sync from a MYOB ADVANCED source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-netsuite-salesforce/`](dlake-crmpro-netsuite-salesforce/SKILL.md) | **Salesforce** specifics for a CRMPro forward sync from a NETSUITE source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-processpro-salesforce/`](dlake-crmpro-processpro-salesforce/SKILL.md) | **Salesforce** specifics for a CRMPro forward sync from a PROCESS PRO source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-qaderp-salesforce/`](dlake-crmpro-qaderp-salesforce/SKILL.md) | **Salesforce** specifics for a CRMPro forward sync from a QAD source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-quickbook-salesforce/`](dlake-crmpro-quickbook-salesforce/SKILL.md) | **Salesforce** specifics for a CRMPro forward sync from a QUICKBOOKS source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-quickbookonline-salesforce/`](dlake-crmpro-quickbookonline-salesforce/SKILL.md) | **Salesforce** specifics for a CRMPro forward sync from a QUICKBOOKS ONLINE source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-quickbooks-salesforce/`](dlake-crmpro-quickbooks-salesforce/SKILL.md) | **Salesforce** specifics for a CRMPro forward sync from a QUICKBOOKS source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-sage100-salesforce/`](dlake-crmpro-sage100-salesforce/SKILL.md) | **Salesforce** specifics for a CRMPro forward sync from a SAGE 100 ERP US source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-sage1002013v5-salesforce/`](dlake-crmpro-sage1002013v5-salesforce/SKILL.md) | **Salesforce** specifics for a CRMPro forward sync from a SAGE1002013V5 source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-sage1002014-salesforce/`](dlake-crmpro-sage1002014-salesforce/SKILL.md) | **Salesforce** specifics for a CRMPro forward sync from a SAGE1002014 source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-sage1002016-salesforce/`](dlake-crmpro-sage1002016-salesforce/SKILL.md) | **Salesforce** specifics for a CRMPro forward sync from a SAGE1002016 source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-sage1002017-salesforce/`](dlake-crmpro-sage1002017-salesforce/SKILL.md) | **Salesforce** specifics for a CRMPro forward sync from a SAGE1002017 source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-sage100france-salesforce/`](dlake-crmpro-sage100france-salesforce/SKILL.md) | **Salesforce** specifics for a CRMPro forward sync from a SAGE 100 FRANCE source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-sage100premium-salesforce/`](dlake-crmpro-sage100premium-salesforce/SKILL.md) | **Salesforce** specifics for a CRMPro forward sync from a SAGE 100 PREMIUM source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-sage100us-salesforce/`](dlake-crmpro-sage100us-salesforce/SKILL.md) | **Salesforce** specifics for a CRMPro forward sync from a SAGE100US source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-sage200evolution-salesforce/`](dlake-crmpro-sage200evolution-salesforce/SKILL.md) | **Salesforce** specifics for a CRMPro forward sync from a SAGE 200 EVOLUTION source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-sage200uk-salesforce/`](dlake-crmpro-sage200uk-salesforce/SKILL.md) | **Salesforce** specifics for a CRMPro forward sync from a SAGE 200 UK source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-sage200us-salesforce/`](dlake-crmpro-sage200us-salesforce/SKILL.md) | **Salesforce** specifics for a CRMPro forward sync from a SAGE 200 US source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-sage300-salesforce/`](dlake-crmpro-sage300-salesforce/SKILL.md) | **Salesforce** specifics for a CRMPro forward sync from a SAGE 300 source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-sage300cre-salesforce/`](dlake-crmpro-sage300cre-salesforce/SKILL.md) | **Salesforce** specifics for a CRMPro forward sync from a SAGE 300 CRE source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-sage500-salesforce/`](dlake-crmpro-sage500-salesforce/SKILL.md) | **Salesforce** specifics for a CRMPro forward sync from a SAGE 500 source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-sage50ca-salesforce/`](dlake-crmpro-sage50ca-salesforce/SKILL.md) | **Salesforce** specifics for a CRMPro forward sync from a SAGE 50 CA source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-sage50uk-salesforce/`](dlake-crmpro-sage50uk-salesforce/SKILL.md) | **Salesforce** specifics for a CRMPro forward sync from a SAGE 50 UK source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-sage50us-salesforce/`](dlake-crmpro-sage50us-salesforce/SKILL.md) | **Salesforce** specifics for a CRMPro forward sync from a SAGE 50 US source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-sagebusinessworks-salesforce/`](dlake-crmpro-sagebusinessworks-salesforce/SKILL.md) | **Salesforce** specifics for a CRMPro forward sync from a SAGE BUSINESS WORKS 2013/2015 source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-sageintacct-salesforce/`](dlake-crmpro-sageintacct-salesforce/SKILL.md) | **Salesforce** specifics for a CRMPro forward sync from a SAGE INTACCT source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-sagemass90-salesforce/`](dlake-crmpro-sagemass90-salesforce/SKILL.md) | **Salesforce** specifics for a CRMPro forward sync from a SAGEMASS90 source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-sagex3-salesforce/`](dlake-crmpro-sagex3-salesforce/SKILL.md) | **Salesforce** specifics for a CRMPro forward sync from a SAGE X3 source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-sapb1-salesforce/`](dlake-crmpro-sapb1-salesforce/SKILL.md) | **Salesforce** specifics for a CRMPro forward sync from a SAP B1 source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-saphana-salesforce/`](dlake-crmpro-saphana-salesforce/SKILL.md) | **Salesforce** specifics for a CRMPro forward sync from a SAP HANA source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-southware-salesforce/`](dlake-crmpro-southware-salesforce/SKILL.md) | **Salesforce** specifics for a CRMPro forward sync from a SOUTHWARE source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-steelviking-salesforce/`](dlake-crmpro-steelviking-salesforce/SKILL.md) | **Salesforce** specifics for a CRMPro forward sync from a STEELVIKING source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-syspro-salesforce/`](dlake-crmpro-syspro-salesforce/SKILL.md) | **Salesforce** specifics for a CRMPro forward sync from a SYSPRO 6 source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-syspro7-salesforce/`](dlake-crmpro-syspro7-salesforce/SKILL.md) | **Salesforce** specifics for a CRMPro forward sync from a SYSPRO 7 and Above source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-traverse-salesforce/`](dlake-crmpro-traverse-salesforce/SKILL.md) | **Salesforce** specifics for a CRMPro forward sync from a TRAVERSE 11 source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-traverseprocessproglobal-salesforce/`](dlake-crmpro-traverseprocessproglobal-salesforce/SKILL.md) | **Salesforce** specifics for a CRMPro forward sync from a TRAVERSE PROCESS PRO GLOBAL source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-vais2k-salesforce/`](dlake-crmpro-vais2k-salesforce/SKILL.md) | **Salesforce** specifics for a CRMPro forward sync from a VAI S2K source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-workday-salesforce/`](dlake-crmpro-workday-salesforce/SKILL.md) | **Salesforce** specifics for a CRMPro forward sync from a WORKDAY source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-xero-salesforce/`](dlake-crmpro-xero-salesforce/SKILL.md) | **Salesforce** specifics for a CRMPro forward sync from a XERO source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |

## Shopify

| Skill | What it covers |
|-------|----------------|
| [`dlake-crmpro-acumatica-shopify/`](dlake-crmpro-acumatica-shopify/SKILL.md) | **Shopify** specifics for a CRMPro forward sync from an ACUMATICA source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-acumaticacloud-shopify/`](dlake-crmpro-acumaticacloud-shopify/SKILL.md) | **Shopify** specifics for a CRMPro forward sync from an ACUMATICA CLOUD source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-dynamicsbusinesscentral-shopify/`](dlake-crmpro-dynamicsbusinesscentral-shopify/SKILL.md) | **Shopify** specifics for a CRMPro forward sync from a DYNAMICS BUSINESS CENTRAL source, from the shipped templates: the configuration values each template inserts, the views it creates and the order the processes run in. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-epicor10-shopify/`](dlake-crmpro-epicor10-shopify/SKILL.md) | **Shopify** specifics for a CRMPro forward sync from an EPICOR 10 source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-epicor10cloud-shopify/`](dlake-crmpro-epicor10cloud-shopify/SKILL.md) | **Shopify** specifics for a CRMPro forward sync from an EPICOR10 CLOUD source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-epicor9-shopify/`](dlake-crmpro-epicor9-shopify/SKILL.md) | **Shopify** specifics for a CRMPro forward sync from an EPICOR 9, EPICOR 9.5 source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-epicorp21-shopify/`](dlake-crmpro-epicorp21-shopify/SKILL.md) | **Shopify** specifics for a CRMPro forward sync from an EPICOR P21(PROPHET 21) source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-microsoftbusinesscentral-shopify/`](dlake-crmpro-microsoftbusinesscentral-shopify/SKILL.md) | **Shopify** specifics for a CRMPro forward sync from a MICROSOFT BUSINESS CENTRAL source, from the shipped templates: the configuration values each template inserts, the views it creates and the order the processes run in. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-msdynamicgp2013-shopify/`](dlake-crmpro-msdynamicgp2013-shopify/SKILL.md) | **Shopify** specifics for a CRMPro forward sync from a MS DYNAMIC GP 2013 source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-msdynamicgp2016-shopify/`](dlake-crmpro-msdynamicgp2016-shopify/SKILL.md) | **Shopify** specifics for a CRMPro forward sync from a MS DYNAMIC GP 2016 source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-msdynamicgp2017-shopify/`](dlake-crmpro-msdynamicgp2017-shopify/SKILL.md) | **Shopify** specifics for a CRMPro forward sync from a MS DYNAMIC GP 2017 source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-quickbook-shopify/`](dlake-crmpro-quickbook-shopify/SKILL.md) | **Shopify** specifics for a CRMPro forward sync from a QUICKBOOKS source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-sage100-shopify/`](dlake-crmpro-sage100-shopify/SKILL.md) | **Shopify** specifics for a CRMPro forward sync from a SAGE 100 ERP US source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-sage1002013v5-shopify/`](dlake-crmpro-sage1002013v5-shopify/SKILL.md) | **Shopify** specifics for a CRMPro forward sync from a SAGE1002013V5 source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-sage1002015-shopify/`](dlake-crmpro-sage1002015-shopify/SKILL.md) | **Shopify** specifics for a CRMPro forward sync from a SAGE1002015 source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-sage1002016-shopify/`](dlake-crmpro-sage1002016-shopify/SKILL.md) | **Shopify** specifics for a CRMPro forward sync from a SAGE1002016 source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-sage1002017-shopify/`](dlake-crmpro-sage1002017-shopify/SKILL.md) | **Shopify** specifics for a CRMPro forward sync from a SAGE1002017 source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-sage1002018-shopify/`](dlake-crmpro-sage1002018-shopify/SKILL.md) | **Shopify** specifics for a CRMPro forward sync from a SAGE1002018 source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-sage50uk-shopify/`](dlake-crmpro-sage50uk-shopify/SKILL.md) | **Shopify** specifics for a CRMPro forward sync from a SAGE 50 UK source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-sage50us-shopify/`](dlake-crmpro-sage50us-shopify/SKILL.md) | **Shopify** specifics for a CRMPro forward sync from a SAGE 50 US source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-sageintacct-shopify/`](dlake-crmpro-sageintacct-shopify/SKILL.md) | **Shopify** specifics for a CRMPro forward sync from a SAGE INTACCT source, from the shipped templates: the configuration values each template inserts and the views it creates. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-syspro-shopify/`](dlake-crmpro-syspro-shopify/SKILL.md) | **Shopify** specifics for a CRMPro forward sync from a SYSPRO 6 source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-syspro7-shopify/`](dlake-crmpro-syspro7-shopify/SKILL.md) | **Shopify** specifics for a CRMPro forward sync from a SYSPRO 7 and Above source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-traverse-shopify/`](dlake-crmpro-traverse-shopify/SKILL.md) | **Shopify** specifics for a CRMPro forward sync from a TRAVERSE 11 source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |

## ZohoCRM

| Skill | What it covers |
|-------|----------------|
| [`dlake-crmpro-acumatica-zohocrm/`](dlake-crmpro-acumatica-zohocrm/SKILL.md) | **ZohoCRM** specifics for a CRMPro forward sync from an ACUMATICA source, from the shipped templates: the configuration values each template inserts, the views it creates and the order the processes run in. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-epicoreagle-zohocrm/`](dlake-crmpro-epicoreagle-zohocrm/SKILL.md) | **ZohoCRM** specifics for a CRMPro forward sync from an EPICOR EAGLE source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-epicorp21-zohocrm/`](dlake-crmpro-epicorp21-zohocrm/SKILL.md) | **ZohoCRM** specifics for a CRMPro forward sync from an EPICOR P21(PROPHET 21) source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-exactonline-zohocrm/`](dlake-crmpro-exactonline-zohocrm/SKILL.md) | **ZohoCRM** specifics for a CRMPro forward sync from an EXACT ONLINE source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-globalshop-zohocrm/`](dlake-crmpro-globalshop-zohocrm/SKILL.md) | **ZohoCRM** specifics for a CRMPro forward sync from a GLOBALSHOP source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-inforcloudsuite-zohocrm/`](dlake-crmpro-inforcloudsuite-zohocrm/SKILL.md) | **ZohoCRM** specifics for a CRMPro forward sync from an INFOR CLOUDSUITE source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-inforvisual9-zohocrm/`](dlake-crmpro-inforvisual9-zohocrm/SKILL.md) | **ZohoCRM** specifics for a CRMPro forward sync from an INFORVISUAL9 source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-msdynamicax-zohocrm/`](dlake-crmpro-msdynamicax-zohocrm/SKILL.md) | **ZohoCRM** specifics for a CRMPro forward sync from a MS DYNAMIC AX source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-msdynamicgp2016-zohocrm/`](dlake-crmpro-msdynamicgp2016-zohocrm/SKILL.md) | **ZohoCRM** specifics for a CRMPro forward sync from a MS DYNAMIC GP 2016 source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-msdynamicgp2017-zohocrm/`](dlake-crmpro-msdynamicgp2017-zohocrm/SKILL.md) | **ZohoCRM** specifics for a CRMPro forward sync from a MS DYNAMIC GP 2017 source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-netsuite-zohocrm/`](dlake-crmpro-netsuite-zohocrm/SKILL.md) | **ZohoCRM** specifics for a CRMPro forward sync from a NETSUITE source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-quickbook-zohocrm/`](dlake-crmpro-quickbook-zohocrm/SKILL.md) | **ZohoCRM** specifics for a CRMPro forward sync from a QUICKBOOKS source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-sage100-zohocrm/`](dlake-crmpro-sage100-zohocrm/SKILL.md) | **ZohoCRM** specifics for a CRMPro forward sync from a SAGE 100 ERP US source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-sage100c2018-zohocrm/`](dlake-crmpro-sage100c2018-zohocrm/SKILL.md) | **ZohoCRM** specifics for a CRMPro forward sync from a SAGE100C2018 source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-sage100contractor2018-zohocrm/`](dlake-crmpro-sage100contractor2018-zohocrm/SKILL.md) | **ZohoCRM** specifics for a CRMPro forward sync from a SAGE100CONTRACTOR2018 source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-sage100premium-zohocrm/`](dlake-crmpro-sage100premium-zohocrm/SKILL.md) | **ZohoCRM** specifics for a CRMPro forward sync from a SAGE 100 PREMIUM source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-sage200uk-zohocrm/`](dlake-crmpro-sage200uk-zohocrm/SKILL.md) | **ZohoCRM** specifics for a CRMPro forward sync from a SAGE 200 UK source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-sage300-zohocrm/`](dlake-crmpro-sage300-zohocrm/SKILL.md) | **ZohoCRM** specifics for a CRMPro forward sync from a SAGE 300 source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-sage50ca-zohocrm/`](dlake-crmpro-sage50ca-zohocrm/SKILL.md) | **ZohoCRM** specifics for a CRMPro forward sync from a SAGE 50 CA source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-sage50uk-zohocrm/`](dlake-crmpro-sage50uk-zohocrm/SKILL.md) | **ZohoCRM** specifics for a CRMPro forward sync from a SAGE 50 UK source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-sage50us-zohocrm/`](dlake-crmpro-sage50us-zohocrm/SKILL.md) | **ZohoCRM** specifics for a CRMPro forward sync from a SAGE 50 US source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-sagex3-zohocrm/`](dlake-crmpro-sagex3-zohocrm/SKILL.md) | **ZohoCRM** specifics for a CRMPro forward sync from a SAGE X3 source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-sapb1-zohocrm/`](dlake-crmpro-sapb1-zohocrm/SKILL.md) | **ZohoCRM** specifics for a CRMPro forward sync from a SAP B1 source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |
| [`dlake-crmpro-syspro7-zohocrm/`](dlake-crmpro-syspro7-zohocrm/SKILL.md) | **ZohoCRM** specifics for a CRMPro forward sync from a SYSPRO 7 and Above source, from the shipped templates: the configuration values each template inserts, the views it creates, the order the processes run in and the field mapping it carries. Extends `dlake-crmpro/`. |

