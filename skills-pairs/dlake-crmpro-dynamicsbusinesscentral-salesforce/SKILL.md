---
name: dlake-crmpro-dynamicsbusinesscentral-salesforce
description: >-
  What the shipped CRMPro templates set up for a DYNAMICS BUSINESS CENTRAL source pushing into
  Salesforce: the `CRM_Configuration` row each template inserts (object API names
  `DYNAMICSBUSINESSCENTRAL_Employee__c`, `Account`, `Contact`,
  `CommercientSF9__MSDYNAMICNAV_CUSTOMER__c`, `CommercientSF9__MSDYNAMICNAV_INVOICEHEADER__c`,
  `CommercientSF9__MSDYNAMICNAV_INVOICELINES__c` and the rest, the match key, `Sync_Order`,
  `TimeStamp_Prefix` and the field prefix/postfix pair), the views they create and how each one
  detects change, the `:` repository key these templates build, and the business outcome of each
  template group — SYNC EMPLOYEE, Account, Invoice, Salesorder. Use it when standing up or reading
  a DYNAMICS BUSINESS CENTRAL → Salesforce template set, when deciding which templates to import
  and activate, or when a run completes without pushing records and the answer is in the view or
  the configuration row. It extends `dlake-crmpro`, which covers operating CRMPro generally, and
  `dlake-crmpro-syspro-salesforce`, which carries the hand-written Salesforce detail.
generated: true
---
<!-- generated by gen-crmpro-skill.js from the shipped template catalogue; hand-edits promote this page to hand-written and it must then be reviewed line by line -->

# CRMPro → Salesforce (DYNAMICS BUSINESS CENTRAL): what the shipped templates set up

> **Keep this skill current.** `dlake` ships updates often and this text is embedded in the CLI
> you have installed. Before relying on it, compare `dlake --version` with
> `npm view @commercient/dlake version`; if they differ, run `npm install -g @commercient/dlake@latest`
> and then `dlake skills install`, which overwrites the installed skill files with the current text.

`dlake-crmpro` is the parent skill and the authority for everything general: the `crmpro_*` tools,
`CRM_Configuration` and `CRM_FieldList`, `TimeStampRepository`, the three kinds of source view,
the NULL-cursor rule and the silent zero-record run. Read it first; this page does not repeat it.
`dlake-crmpro-syspro-salesforce` is the hand-written Salesforce page and carries the
destination-side detail for this CRM; read it alongside this one. What follows is only what the
shipped DYNAMICS BUSINESS CENTRAL → Salesforce templates themselves set, read from their
`Insert_Query`, `CreateViewQuery` and `MappingJson`. This page grows as the catalogue does.

## 1. What the templates deliver

| Group | Business outcome | Objects | Source tables and views |
|---|---|---|---|
| **SYNC EMPLOYEE** | ERP `EMPLOYEE` data becomes `DYNAMICSBUSINESSCENTRAL_Employee__c` in Salesforce. New records are created, existing ones updated, and records removed in the ERP are deleted. | `DYNAMICSBUSINESSCENTRAL_Employee__c` | `SF_ERP_Salesforce_Clone_Employee` |
| **Account** | In addition to creating the Account record from your ERP customer records, Commercient syncs over the entire Accounting AR Customer record into a Commercient AR Customer object which is a Managed Custom Object (MCO). A lookup field is provided to lookup to the default AR Customer record MCO from the Account record. New records are created, existing ones updated, and records removed in the ERP are deleted. | `Account`, `Contact`, `CommercientSF9__MSDYNAMICNAV_CUSTOMER__c` | `SF_ERP_Salesforce_Clone_Customer`, `SF_ERP_Salesforce_Clone_Contacts` |
| **Invoice** | Both Open and Historical invoices are synchronized. Open invoice changes, such as a balance change, or a terms change are reflected in the CRM as they sync based on the frequency of the sync you have chosen (e.g. every hour for example). New records are created, existing ones updated, and records removed in the ERP are deleted. | `CommercientSF9__MSDYNAMICNAV_INVOICEHEADER__c`, `CommercientSF9__MSDYNAMICNAV_INVOICELINES__c` | `SF_ERP_Salesforce_Clone_SalesInvoice`, `SF_ERP_Salesforce_Clone_SalesInvoiceLine` |
| **Salesorder** | Commercient will sync the Sales Orders from the ERP Sales Order Entry module to the Commercient Sales Order Header (MCO) objects in CRM. In the CRM, customer service and sales people can visualize the status of the order such as on hold, backorder, forward order, scheduled for delivery, whether it has shipped, and completion status. New records are created, existing ones updated, and records removed in the ERP are deleted. | `CommercientSF9__MSDYNAMICNAV_ORDERHEADER__c`, `CommercientSF9__MSDYNAMICNAV_ORDERLINES__c` | `SF_ERP_Salesforce_Clone_SalesOrder`, `CommercientFlags`, `SF_ERP_Salesforce_Clone_SalesOrderLine` |

## 2. The process rows the templates create

Each template's `Insert_Query` writes one `CRM_Configuration` row. These are the values it sets; a
column the inserts never set is not listed.

| Display name | `CRM_Object_API_Name` | `CRM_PK_API_Name` | `Sync_Order` | `TimeStamp_Prefix` | `SQL_Query` target | field prefix / postfix | `Sync_Operation_Type` |
|---|---|---|---|---|---|---|---|
| SYNC EMPLOYEE | `DYNAMICSBUSINESSCENTRAL_Employee__c` | `ExternalKey__c` | 0 | `vw_DYNAMICSBUSINESSCENTRAL_Employee` | `vw_DYNAMICSBUSINESSCENTRAL_Employee` | `''` / `__c` | `1` |
| SYNC ACCOUNT | `Account` | `CommercientSF__Commercient_ArCustomerCode__c` | 1 | `vw_DYNAMICSBUSINESSCENTRAL_Account` | `vw_DYNAMICSBUSINESSCENTRAL_Account` | `''` / `''` | `1` |
| SYNC CUSTOMER | `CommercientSF9__MSDYNAMICNAV_CUSTOMER__c` | `CommercientSF9__ExternalKey__c` | 2 | `vw_DYNAMICSBUSINESSCENTRAL_Customer` | `vw_DYNAMICSBUSINESSCENTRAL_Customer` | `CommercientSF9__` / `__c` | `1` |
| SYNC CUSTOMERTOACCOUNTLOOKUP | `Account` | `CommercientSF__Commercient_ArCustomerCode__c` | 3 | `vw_DYNAMICSBUSINESSCENTRAL_CustomerToAccountLookup` | `vw_DYNAMICSBUSINESSCENTRAL_CustomerToAccountLookup` | `''` / `''` | `1` |
| SYNC CONTACTS | `Contact` | `ExternalKey__c` | 4 | `vw_DYNAMICSBUSINESSCENTRAL_Contacts` | `vw_DYNAMICSBUSINESSCENTRAL_Contacts` | `''` / `__c` | `1` |
| SYNC SALESORDERHEADER | `CommercientSF9__MSDYNAMICNAV_ORDERHEADER__c` | `CommercientSF9__ExternalKey__c` | 10 | `vw_DYNAMICSBUSINESSCENTRAL_SalesOrderHeader` | `vw_DYNAMICSBUSINESSCENTRAL_SalesOrderHeader` | `CommercientSF9__` / `__c` | `1` |
| SYNC SALESORDERDETAIL | `CommercientSF9__MSDYNAMICNAV_ORDERLINES__c` | `CommercientSF9__ExternalKey__c` | 11 | `vw_DYNAMICSBUSINESSCENTRAL_SalesOrderDetail` | `vw_DYNAMICSBUSINESSCENTRAL_SalesOrderDetail` | `CommercientSF9__` / `__c` | `1` |
| SYNC INVOICEHEADER | `CommercientSF9__MSDYNAMICNAV_INVOICEHEADER__c` | `CommercientSF9__ExternalKey__c` | 12 | `vw_DYNAMICSBUSINESSCENTRAL_InvoiceHeader` | `vw_DYNAMICSBUSINESSCENTRAL_InvoiceHeader` | `CommercientSF9__` / `__c` | `1` |
| SYNC INVOICEDETAIL | `CommercientSF9__MSDYNAMICNAV_INVOICELINES__c` | `CommercientSF9__ExternalKey__c` | 13 | `vw_DYNAMICSBUSINESSCENTRAL_InvoiceDetail` | `vw_DYNAMICSBUSINESSCENTRAL_InvoiceDetail` | `CommercientSF9__` / `__c` | `1` |

Every one of these inserts `Is_Active` as `0`, so an imported process is inactive until an
operator activates it. Across the set they insert `Is_Create_Entity` `0`, `Is_Create_Fields` `0`,
`Is_Active_Get_Records` `0`, `Is_Active_Delete_Records` `0`, `IsAccountMatching` `False`,
`Sync_Batch_Size` `200`. Text columns a template does not use are inserted as `''` rather than
left NULL.

## 3. The views

Each template creates its view with `CreateViewQuery`, in the gateway `dbo` schema. "Change
detection" names which of the three kinds `dlake-crmpro` defines the view's own `WHERE` makes it;
where a view's shape is not one of those three the column is left empty and the view's own `WHERE`
is the authority.

| View | Change detection | Repository key | Reads other prefixes | Identity column | Source tables |
|---|---|---|---|---|---|
| `vw_DYNAMICSBUSINESSCENTRAL_Employee` | insert + update | `vw_DYNAMICSBUSINESSCENTRAL_Employee:` | — | — | `SF_ERP_Salesforce_Clone_Employee` |
| `vw_DYNAMICSBUSINESSCENTRAL_Account` | insert + update | `vw_DYNAMICSBUSINESSCENTRAL_Account:` | — | `CommercientSF__Commercient_ArCustomerCode__c` | `SF_ERP_Salesforce_Clone_Customer` |
| `vw_DYNAMICSBUSINESSCENTRAL_Customer` | insert + update | `vw_DYNAMICSBUSINESSCENTRAL_Customer:` | `vw_DYNAMICSBUSINESSCENTRAL_Account:` | `ExternalKey` | `SF_ERP_Salesforce_Clone_Customer` |
| `vw_DYNAMICSBUSINESSCENTRAL_CustomerToAccountLookup` | insert + update | `vw_DYNAMICSBUSINESSCENTRAL_CustomerToAccountLookup:` | `vw_DYNAMICSBUSINESSCENTRAL_Customer:` | — | `SF_ERP_Salesforce_Clone_Customer` |
| `vw_DYNAMICSBUSINESSCENTRAL_Contacts` | insert + update | `vw_DYNAMICSBUSINESSCENTRAL_Contacts:` | `vw_DYNAMICSBUSINESSCENTRAL_Account:`, `vw_DYNAMICSBUSINESSCENTRAL_Customer:` | — | `SF_ERP_Salesforce_Clone_Contacts` |
| `vw_DYNAMICSBUSINESSCENTRAL_SalesOrderHeader` | insert + update | `vw_DYNAMICSBUSINESSCENTRAL_SalesOrderHeader:` | `vw_DYNAMICSBUSINESSCENTRAL_Account:`, `vw_DYNAMICSBUSINESSCENTRAL_Customer:` | `ExternalKey` | `SF_ERP_Salesforce_Clone_SalesOrder`, `CommercientFlags` |
| `vw_DYNAMICSBUSINESSCENTRAL_SalesOrderDetail` | insert + update | `vw_DYNAMICSBUSINESSCENTRAL_SalesOrderDetail:` | `vw_DYNAMICSBUSINESSCENTRAL_SalesOrderHeader:` | `ExternalKey` | `SF_ERP_Salesforce_Clone_SalesOrderLine` |
| `vw_DYNAMICSBUSINESSCENTRAL_InvoiceHeader` | insert + update | `vw_DYNAMICSBUSINESSCENTRAL_InvoiceHeader:` | `vw_DYNAMICSBUSINESSCENTRAL_Account:`, `vw_DYNAMICSBUSINESSCENTRAL_Customer:` | — | `SF_ERP_Salesforce_Clone_SalesInvoice` |
| `vw_DYNAMICSBUSINESSCENTRAL_InvoiceDetail` | insert + update | `vw_DYNAMICSBUSINESSCENTRAL_InvoiceDetail:` | `vw_DYNAMICSBUSINESSCENTRAL_InvoiceHeader:` | — | `SF_ERP_Salesforce_Clone_SalesInvoiceLine` |

The repository key these templates build is `<TimeStamp_Prefix>:<key>`. The literal in the view
and the `TimeStamp_Prefix` on the configuration row have to be the same string, case included.

## 4. Order of work

The templates set `Sync_Order` from `0` to `13`. A run processes active rows in ascending
`Sync_Order`, which is the order the templates put them in:

- `0` — SYNC EMPLOYEE
- `1` — SYNC ACCOUNT
- `2` — SYNC CUSTOMER
- `3` — SYNC CUSTOMERTOACCOUNTLOOKUP
- `4` — SYNC CONTACTS
- `10` — SYNC SALESORDERHEADER
- `11` — SYNC SALESORDERDETAIL
- `12` — SYNC INVOICEHEADER
- `13` — SYNC INVOICEDETAIL

These views read another process's repository prefix, which is what makes the order a dependency
order: the row appears in the view only once the process that writes that prefix has run, so a
parent flows on one run and its children on the next.

- `vw_DYNAMICSBUSINESSCENTRAL_CustomerToAccountLookup` reads `vw_DYNAMICSBUSINESSCENTRAL_Customer:`
- `vw_DYNAMICSBUSINESSCENTRAL_Contacts` reads `vw_DYNAMICSBUSINESSCENTRAL_Account:`, `vw_DYNAMICSBUSINESSCENTRAL_Customer:`
- `vw_DYNAMICSBUSINESSCENTRAL_Customer` reads `vw_DYNAMICSBUSINESSCENTRAL_Account:`
- `vw_DYNAMICSBUSINESSCENTRAL_InvoiceHeader` reads `vw_DYNAMICSBUSINESSCENTRAL_Account:`, `vw_DYNAMICSBUSINESSCENTRAL_Customer:`
- `vw_DYNAMICSBUSINESSCENTRAL_InvoiceDetail` reads `vw_DYNAMICSBUSINESSCENTRAL_InvoiceHeader:`
- `vw_DYNAMICSBUSINESSCENTRAL_SalesOrderHeader` reads `vw_DYNAMICSBUSINESSCENTRAL_Account:`, `vw_DYNAMICSBUSINESSCENTRAL_Customer:`
- `vw_DYNAMICSBUSINESSCENTRAL_SalesOrderDetail` reads `vw_DYNAMICSBUSINESSCENTRAL_SalesOrderHeader:`

## 5. Field mapping

`CRM_FieldList` carries one row per pushed column, with `Object_Name` equal to the
`CRM_Object_API_Name` value — as `dlake-crmpro` puts it, an object with no `CRM_FieldList` rows
pushes nothing and records no error. Each template carries its intended mapping in `MappingJson`.
Read that with `dlake admin crmpro_templates`, then check `crmpro_field_mapping` on the created
process before activating it.

| Template | Object | Mapped fields | First ERP → Salesforce pairs |
|---|---|---|---|
| SYNC EMPLOYEE | `DYNAMICSBUSINESSCENTRAL_Employee__c` | 4 | `Code` → `Code__c`, `Commission_Percent` → `Commission_Percent`, `Phone_No` → `Phone_No__c`, `Privacy_Blocked` → `Privacy_Blocked` |
| SYNC ACCOUNT | `Account` | 8 | `CommercientSFommercient_ArCustomerCode` → `CommercientSF__Commercient_ArCustomerCode__c`, `BillingStreet` → `BillingStreet`, `BillingCity` → `BillingCity`, `BillingState` → `BillingState`, `BillingCountry` → `BillingCountry` |
| SYNC CUSTOMER | `CommercientSF9__MSDYNAMICNAV_CUSTOMER__c` | 103 | `Name_2` → `name_2__c`, `Search_Name` → `search_name__c`, `IC_Partner_Code` → `ic_partner_code__c`, `Balance_LCY` → `balance_lcy__c`, `Balance_Due_LCY` → `balance_due_lcy__c` |
| SYNC CUSTOMERTOACCOUNTLOOKUP | `Account` | 2 | `CommercientSFommercient_ArCustomerCode` → `CommercientSF__Commercient_ArCustomerCode__c`, `CommercientSF9__MSDYNAMICS_NAV_Customers` → `CommercientSF9__MSDYNAMICS_NAV_Customers__c` |
| SYNC CONTACTS | `Contact` | 8 | `Name` → `Name__c`, `Type` → `Type__c`, `Title` → `Title__c`, `Phone` → `Phone__c`, `Email` → `Email__c` |
| SYNC SALESORDERHEADER | `CommercientSF9__MSDYNAMICNAV_ORDERHEADER__c` | 98 | `Document_Type` → `document_type__c`, `Sell_to_Customer_No` → `sell_to_customer_no__c`, `Sell_to_Customer_Name` → `sell_to_customer_name__c`, `Quote_No` → `quote_no__c`, `Posting_Description` → `posting_description__c` |
| SYNC SALESORDERDETAIL | `CommercientSF9__MSDYNAMICNAV_ORDERLINES__c` | 97 | `Document_Type` → `document_type__c`, `Document_No` → `document_no__c`, `Line_No` → `line_no__c`, `FilteredTypeField` → `filteredtypefield__c`, `No` → `CommercientSF9__No__c` |
| SYNC INVOICEHEADER | `CommercientSF9__MSDYNAMICNAV_INVOICEHEADER__c` | 84 | `Document_Type` → `document_type__c`, `No` → `CommercientSF9__No__c`, `Sell_to_Customer_No` → `sell_to_customer_no__c`, `Sell_to_Customer_Name` → `sell_to_customer_name__c`, `Posting_Description` → `posting_description__c` |
| SYNC INVOICEDETAIL | `CommercientSF9__MSDYNAMICNAV_INVOICELINES__c` | 71 | `Document_Type` → `document_type__c`, `Document_No` → `document_no__c`, `Line_No` → `line_no__c`, `FilteredTypeField` → `filteredtypefield__c`, `No` → `CommercientSF9__No__c` |

## 6. Verifying

```bash
# per-prefix counts; every synced record carries its destination id
dlake tool query --profile <tenant> --sql "SELECT LEFT([Key], CHARINDEX(':',[Key])-1) AS prefix, COUNT(*) n, COUNT(NULLIF(SFDCID,'')) withId FROM dbo.TimeStampRepository WHERE CHARINDEX(':',[Key])>0 GROUP BY LEFT([Key], CHARINDEX(':',[Key])-1)"
```

`withId = n` for every prefix is the success condition `dlake-crmpro` defines. The split is on the
first colon, which is the prefix boundary whether the key separator is `:` or `::`.

The prefixes these templates set:

- `vw_DYNAMICSBUSINESSCENTRAL_Employee`
- `vw_DYNAMICSBUSINESSCENTRAL_Account`
- `vw_DYNAMICSBUSINESSCENTRAL_CustomerToAccountLookup`
- `vw_DYNAMICSBUSINESSCENTRAL_Contacts`
- `vw_DYNAMICSBUSINESSCENTRAL_Customer`
- `vw_DYNAMICSBUSINESSCENTRAL_InvoiceHeader`
- `vw_DYNAMICSBUSINESSCENTRAL_InvoiceDetail`
- `vw_DYNAMICSBUSINESSCENTRAL_SalesOrderHeader`
- `vw_DYNAMICSBUSINESSCENTRAL_SalesOrderDetail`

## 7. Where this sits

`dlake-crmpro` is the general operating surface — the `crmpro_*` tools, the setup and transaction
tables, field mapping, and the source-view contract that applies to every destination. This page
adds what the shipped DYNAMICS BUSINESS CENTRAL → Salesforce templates set up.
`dlake-crmpro-syspro-salesforce` is the hand-written page for this destination.
`dlake-crmpro-hubspot` and `dlake-crmpro-syspro-shopify` do the same for their destinations, and
the conventions genuinely differ between them. For the extract leg that fills the clone tables,
see `dlake-normalsync`; for the on-premises agent that runs it, `dlake-syncagent`; for the
writeback leg, `dlake-txdownloaderpro`; for standing an integration up, `dlake-integration-setup`.
