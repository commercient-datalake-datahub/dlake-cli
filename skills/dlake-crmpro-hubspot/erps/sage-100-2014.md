---
name: dlake-crmpro-hubspot/erps/sage-100-2014
description: >-
  What the shipped CRMPro templates set up for a Sage 100 2014 source pushing into HubSpot: the
  `CRM_Configuration` row each template inserts (object API names `company`, `products`, `deal`,
  `line_item`, the match key, `Sync_Order`, `TimeStamp_Prefix` and the field prefix/postfix pair),
  the views they create and how each one detects change, the `::` repository key these templates
  build, and the business outcome of each template group — create customer, create item, create
  order, create orderdetail, create invoice, create invoicedetail, create quote, create
  quotedetail and more. Use it when standing up or reading a Sage 100 2014 → HubSpot template set,
  when deciding which templates to import and activate, or when a run completes without pushing
  records and the answer is in the view or the configuration row. It extends `dlake-crmpro`, which
  covers operating CRMPro generally, and `dlake-crmpro-hubspot`, the destination skill this page
  is a child of, which carries the HubSpot conventions that hold across every ERP.
generated: true
---
<!-- generated by gen-crmpro-skill.js from the shipped template catalogue; hand-edits promote this page to hand-written and it must then be reviewed line by line -->

# CRMPro → HubSpot — Sage 100 2014: what the shipped templates set up

> **Keep this skill current.** `dlake` ships updates often and this text is embedded in the CLI
> you have installed. Before relying on it, compare `dlake --version` with
> `npm view @commercient/dlake version`; if they differ, run `npm install -g @commercient/dlake@latest`
> and then `dlake skills install`, which overwrites the installed skill files with the current text.

`dlake-crmpro` is the parent skill and the authority for everything general: the `crmpro_*` tools,
`CRM_Configuration` and `CRM_FieldList`, `TimeStampRepository`, the three kinds of source view,
the NULL-cursor rule and the silent zero-record run. Read it first; this page does not repeat it.
`dlake-crmpro-hubspot` is the destination skill this page is a child of, and the authority for the
HubSpot conventions that hold across every ERP: read it first, then come back here for what this
source's own templates set. This page is `dlake-crmpro-hubspot/erps/sage-100-2014.md`, and that
skill's ERP table is what points at it. What follows is only what the shipped Sage 100 2014 →
HubSpot templates themselves set, read from their `Insert_Query`, `CreateViewQuery` and
`MappingJson`. This page grows as the catalogue does.

## 1. What the templates deliver

| Group | Business outcome | Objects | Source tables and views |
|---|---|---|---|
| **create customer** | ERP `SF_ERP_Salesforce_Clone_AR_CUSTOMER` data becomes `company` in HubSpot. New records are created and existing ones updated; none are deleted. | `company` | `SF_ERP_Salesforce_Clone_AR_CUSTOMER` |
| **create item** | The templates push `products` to HubSpot. New records are created and existing ones updated; none are deleted. | `products` | `SF_ERP_Salesforce_Clone_CI_ITEM` |
| **create order** | The templates push `deal` to HubSpot. New records are created and existing ones updated; none are deleted. | `deal` | `SF_ERP_Salesforce_Clone_SO_SalesOrderHistoryHeader` |
| **create orderdetail** | The templates push `line_item` to HubSpot. New records are created and existing ones updated; none are deleted. | `line_item` | `SF_ERP_Salesforce_Clone_SO_SalesOrderHistoryDetail`, `SF_ERP_Salesforce_Clone_CI_ITEM` |
| **create invoice** | The templates push `deal` to HubSpot. New records are created and existing ones updated; none are deleted. | `deal` | `SF_ERP_Salesforce_Clone_AR_INVOICEHISTORYHEADER` |
| **create invoicedetail** | The templates push `line_item` to HubSpot. New records are created and existing ones updated; none are deleted. | `line_item` | `SF_ERP_Salesforce_Clone_AR_INVOICEHISTORYDETAIL`, `SF_ERP_Salesforce_Clone_CI_ITEM` |
| **create quote** | The templates push `deal` to HubSpot. New records are created and existing ones updated; none are deleted. | `deal` | `SF_ERP_Salesforce_Clone_SO_SALESORDERHEADER` |
| **create quotedetail** | The templates push `line_item` to HubSpot. New records are created and existing ones updated; none are deleted. | `line_item` | `SF_ERP_Salesforce_Clone_SO_SALESORDERDETAIL`, `SF_ERP_Salesforce_Clone_CI_ITEM` |
| **create openinvoice** | The templates push `deal` to HubSpot. New records are created and existing ones updated; none are deleted. | `deal` | `SF_ERP_Salesforce_Clone_AR_OPENINVOICE`, `SF_ERP_Salesforce_Clone_AR_CUSTOMER` |
| **create openinvoicedetail** | The templates push `line_item` to HubSpot. New records are created and existing ones updated; none are deleted. | `line_item` | `SF_ERP_Salesforce_Clone_AR_INVOICEHISTORYDETAIL`, `SF_ERP_Salesforce_Clone_CI_ITEM` |

## 2. The process rows the templates create

Each template's `Insert_Query` writes one `CRM_Configuration` row. These are the values it sets; a
column the inserts never set is not listed.

| Display name | `CRM_Object_API_Name` | `Sync_Order` | `TimeStamp_Prefix` | `SQL_Query` target | field prefix / postfix | `Sync_Operation_Type` |
|---|---|---|---|---|---|---|
| create customer | `company` | 5 | `HUBSPOT_NEW_CUSTOMER` | `vw_HUBSPOT_NEW_CUSTOMER` | `''` / `''` | `2` |
| create item | `products` | 10 | `HUBSPOT_NEW_ITEM` | `vw_HUBSPOT_NEW_ITEM` | `''` / `''` | `2` |
| create order | `deal` | 11 | `HUBSPOT_NEW_DEAL` | `vw_HUBSPOT_NEW_ORDER_DEAL` | `''` / `''` | `2` |
| create orderdetail | `line_item` | 12 | `HUBSPOT_NEW_LINEITEM` | `vw_HUBSPOT_NEW_ORDER_LINEITEM` | `''` / `''` | `2` |
| create invoice | `deal` | 13 | `HUBSPOT_NEW_DEAL` | `vw_HUBSPOT_NEW_INVOICE_DEAL` | `''` / `''` | `2` |
| create invoicedetail | `line_item` | 14 | `HUBSPOT_NEW_LINEITEM` | `vw_HUBSPOT_NEW_INVOICE_LINEITEM` | `''` / `''` | `2` |
| create quote | `deal` | 15 | `HUBSPOT_NEW_DEAL` | `vw_HUBSPOT_NEW_QUOTE_DEAL` | `''` / `''` | `2` |
| create quotedetail | `line_item` | 16 | `HUBSPOT_NEW_LINEITEM` | `vw_HUBSPOT_NEW_QUOTE_LINEITEM` | `''` / `''` | `2` |
| create openinvoice | `deal` | 17 | `HUBSPOT_NEW_DEAL` | `vw_HUBSPOT_NEW_OPENINVOICE_DEAL` | `''` / `''` | `2` |
| create openinvoicedetail | `line_item` | 18 | `HUBSPOT_NEW_LINEITEM` | `vw_HUBSPOT_NEW_OPENINVOICE_LINEITEM` | `''` / `''` | `2` |

Every one of these inserts `Is_Active` as `0`, so an imported process is inactive until an
operator activates it. Across the set they insert `Is_Create_Entity` `0`, `Is_Create_Fields` `0`,
`Is_Active_Get_Records` `0`, `Is_Active_Delete_Records` `0`, `IsAccountMatching` `''`,
`Sync_Batch_Size` `200`. Text columns a template does not use are inserted as `''` rather than
left NULL.

## 3. The views

Each template creates its view with `CreateViewQuery`, in the gateway `dbo` schema. "Change
detection" names which of the three kinds `dlake-crmpro` defines the view's own `WHERE` makes it;
where a view's shape is not one of those three the column is left empty and the view's own `WHERE`
is the authority.

| View | Change detection | Repository key | Reads other prefixes | Identity column | Source tables |
|---|---|---|---|---|---|
| `vw_HUBSPOT_NEW_CUSTOMER` | insert-only | `HUBSPOT_NEW_CUSTOMER::` | — | `RecordKey` | `SF_ERP_Salesforce_Clone_AR_CUSTOMER` |
| `vw_HUBSPOT_NEW_ITEM` | insert-only | `HUBSPOT_NEW_ITEM::` | — | `RecordKey`, `externalkey` | `SF_ERP_Salesforce_Clone_CI_ITEM` |
| `vw_HUBSPOT_NEW_ORDER_DEAL` | insert-only | — | `HUBSPOT_NEW_CUSTOMER::`, `ORDER:` | `RecordKey`, `externalkey` | `SF_ERP_Salesforce_Clone_SO_SalesOrderHistoryHeader` |
| `vw_HUBSPOT_NEW_ORDER_LINEITEM` | insert-only | — | `HUBSPOT_NEW_ITEM::`, `ORDER:` | `RecordKey`, `externalkey` | `SF_ERP_Salesforce_Clone_SO_SalesOrderHistoryDetail`, `SF_ERP_Salesforce_Clone_CI_ITEM` |
| `vw_HUBSPOT_NEW_INVOICE_DEAL` | insert-only | — | `HUBSPOT_NEW_CUSTOMER::`, `INVOICE:` | `RecordKey`, `externalkey` | `SF_ERP_Salesforce_Clone_AR_INVOICEHISTORYHEADER` |
| `vw_HUBSPOT_NEW_INVOICE_LINEITEM` | insert-only | — | `HUBSPOT_NEW_ITEM::`, `INVOICE:` | `RecordKey`, `externalkey` | `SF_ERP_Salesforce_Clone_AR_INVOICEHISTORYDETAIL`, `SF_ERP_Salesforce_Clone_CI_ITEM` |
| `vw_HUBSPOT_NEW_QUOTE_DEAL` | insert-only | — | `HUBSPOT_NEW_CUSTOMER::`, `QUOTE:` | `RecordKey`, `externalkey` | `SF_ERP_Salesforce_Clone_SO_SALESORDERHEADER` |
| `vw_HUBSPOT_NEW_QUOTE_LINEITEM` | insert-only | — | `HUBSPOT_NEW_ITEM::`, `QUOTE:` | `RecordKey`, `externalkey` | `SF_ERP_Salesforce_Clone_SO_SALESORDERDETAIL`, `SF_ERP_Salesforce_Clone_CI_ITEM` |
| `vw_HUBSPOT_NEW_OPENINVOICE_DEAL` | insert-only | — | `HUBSPOT_NEW_CUSTOMER::`, `OPENINVOICE:` | `RecordKey`, `externalkey` | `SF_ERP_Salesforce_Clone_AR_OPENINVOICE`, `SF_ERP_Salesforce_Clone_AR_CUSTOMER` |
| `vw_HUBSPOT_NEW_OPENINVOICE_LINEITEM` | insert-only | — | `HUBSPOT_NEW_ITEM::`, `OPENINVOICE:` | `RecordKey`, `externalkey` | `SF_ERP_Salesforce_Clone_AR_INVOICEHISTORYDETAIL`, `SF_ERP_Salesforce_Clone_CI_ITEM` |

The repository key these templates build is `<TimeStamp_Prefix>::<key>`. The literal in the view
and the `TimeStamp_Prefix` on the configuration row have to be the same string, case included.

## 4. Order of work

The templates set `Sync_Order` from `5` to `18`. A run processes active rows in ascending
`Sync_Order`, which is the order the templates put them in:

- `5` — create customer
- `10` — create item
- `11` — create order
- `12` — create orderdetail
- `13` — create invoice
- `14` — create invoicedetail
- `15` — create quote
- `16` — create quotedetail
- `17` — create openinvoice
- `18` — create openinvoicedetail

These views read another process's repository prefix, which is what makes the order a dependency
order: the row appears in the view only once the process that writes that prefix has run, so a
parent flows on one run and its children on the next.

- `vw_HUBSPOT_NEW_ORDER_DEAL` reads `HUBSPOT_NEW_CUSTOMER::`, `ORDER:`; no template in this set writes `ORDER:`
- `vw_HUBSPOT_NEW_ORDER_LINEITEM` reads `HUBSPOT_NEW_ITEM::`, `ORDER:`; no template in this set writes `ORDER:`
- `vw_HUBSPOT_NEW_INVOICE_DEAL` reads `HUBSPOT_NEW_CUSTOMER::`, `INVOICE:`; no template in this set writes `INVOICE:`
- `vw_HUBSPOT_NEW_INVOICE_LINEITEM` reads `HUBSPOT_NEW_ITEM::`, `INVOICE:`; no template in this set writes `INVOICE:`
- `vw_HUBSPOT_NEW_QUOTE_DEAL` reads `HUBSPOT_NEW_CUSTOMER::`, `QUOTE:`; no template in this set writes `QUOTE:`
- `vw_HUBSPOT_NEW_QUOTE_LINEITEM` reads `HUBSPOT_NEW_ITEM::`, `QUOTE:`; no template in this set writes `QUOTE:`
- `vw_HUBSPOT_NEW_OPENINVOICE_DEAL` reads `HUBSPOT_NEW_CUSTOMER::`, `OPENINVOICE:`; no template in this set writes `OPENINVOICE:`
- `vw_HUBSPOT_NEW_OPENINVOICE_LINEITEM` reads `HUBSPOT_NEW_ITEM::`, `OPENINVOICE:`; no template in this set writes `OPENINVOICE:`

## 5. Field mapping

`CRM_FieldList` carries one row per pushed column, with `Object_Name` equal to the
`CRM_Object_API_Name` value — as `dlake-crmpro` puts it, an object with no `CRM_FieldList` rows
pushes nothing and records no error. Each template carries its intended mapping in `MappingJson`.
Read that with `dlake admin crmpro_templates`, then check `crmpro_field_mapping` on the created
process before activating it.

| Template | Object | Mapped fields | First ERP → HubSpot pairs |
|---|---|---|---|
| create customer | `company` | 9 | `CUSTOMERNO` → `arcustomercode`, `CUSTOMERNAME` → `name`, `ADDRESSLINE1` → `address`, `ADDRESSLINE2` → `address2`, `CITY` → `city` |
| create item | `products` | 5 | `ITEMCODE` → `hs_sku`, `ITEMCODEDESC` → `name`, `ITEMCODEDESC` → `description`, `STANDARDUNITCOST` → `hs_cost_of_goods_sold`, `STANDARDUNITPRICE` → `price` |
| create order | `deal` | 8 | `SALESORDERNO` → `externalkey`, `CloseDate` → `closedate`, `CloseDate` → `createdate`, `BILLTONAME, SALESORDERNO` → `dealname`, `CUSTOMERNO` → `associate_company` |
| create orderdetail | `line_item` | 6 | `LASTUNITPRICE` → `price`, `QUANTITYBACKORDERED` → `quantity`, `SFDCID` → `hs_product_id`, `ITEMCODE` → `name`, `SFDCID` → `associate_deal` |
| create invoice | `deal` | 10 | `INVOICENO` → `RecordKey`, `INVOICENO` → `externalkey`, `CloseDate` → `closedate`, `CloseDate` → `createdate`, `TimeStamp` → `Timestamp` |
| create invoicedetail | `line_item` | 6 | `UNITPRICE` → `price`, `QUANTITYORDERED` → `quantity`, `SFDCID` → `hs_product_id`, `ITEMCODE` → `name`, `SFDCID` → `associate_deal` |
| create quote | `deal` | 8 | `SALESORDERNO` → `externalkey`, `CloseDate` → `closedate`, `CloseDate` → `createdate`, `BILLTONAME, SALESORDERNO` → `dealname`, `SFDCID` → `associate_company` |
| create quotedetail | `line_item` | 6 | `UNITPRICE` → `price`, `QUANTITYBACKORDERED` → `quantity`, `SFDCID` → `hs_product_id`, `ITEMCODE` → `name`, `SFDCID` → `associate_deal` |
| create openinvoice | `deal` | 8 | `INVOICENO` → `externalkey`, `CloseDate` → `closedate`, `CloseDate` → `createdate`, `CUSTOMERNAME, INVOICENO` → `dealname`, `SFDCID` → `associate_company` |
| create openinvoicedetail | `line_item` | 5 | `ITEMCODE` → `hs_product_id`, `INVOICENO` → `associate_deal`, `UNITPRICE` → `price`, `QUANTITYORDERED` → `quantity`, `ITEMCODE` → `name` |

## 6. Verifying

```bash
# per-prefix counts; every synced record carries its destination id
dlake tool query --profile <tenant> --sql "SELECT LEFT([Key], CHARINDEX(':',[Key])-1) AS prefix, COUNT(*) n, COUNT(NULLIF(SFDCID,'')) withId FROM dbo.TimeStampRepository WHERE CHARINDEX(':',[Key])>0 GROUP BY LEFT([Key], CHARINDEX(':',[Key])-1)"
```

`withId = n` for every prefix is the success condition `dlake-crmpro` defines. The split is on the
first colon, which is the prefix boundary whether the key separator is `:` or `::`.

The prefixes these templates set:

- `HUBSPOT_NEW_CUSTOMER`
- `HUBSPOT_NEW_ITEM`
- `HUBSPOT_NEW_DEAL`
- `HUBSPOT_NEW_LINEITEM`

## 7. Where this sits

`dlake-crmpro` is the general operating surface — the `crmpro_*` tools, the setup and transaction
tables, field mapping, and the source-view contract that applies to every destination. This page
adds what the shipped Sage 100 2014 → HubSpot templates set up. `dlake-crmpro-hubspot` is the
destination skill this page sits under: its own text is the authority for the HubSpot conventions
that hold across every ERP, and its ERP table lists this page alongside every sibling ERP page for
this destination. For the extract leg that fills the clone tables, see `dlake-normalsync`; for the
on-premises agent that runs it, `dlake-syncagent`; for the writeback leg, `dlake-txdownloaderpro`;
for standing an integration up, `dlake-integration-setup`.
